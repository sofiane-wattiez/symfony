{% extends 'base.html.twig' %}


{% block title %}Article
{% endblock %}

{% block body %}


	{% for article in articles  %}
		{# {{article.title}} #}
		<div class="article text-center">
			<h1 class="text-center">
				{{article.titre}}</h1>
			{# <p class="text-center"> {{article.titre}}</p> #}
			<div class="article-image">
				<img src={{article.getImageArticle}} alt="">
				<div class="contenu">
					<h1>Content</h1>
					<p>{{article.contenu}}</p>

				</div>
			</div>
		</div>
	{% endfor %}


{% endblock %}
